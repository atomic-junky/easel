name: Dev build

on:
  pull_request:

concurrency:
  group: dev-build-${{ github.ref }}
  cancel-in-progress: true

env:
  GODOT_VERSION: 4.7.1
  EXPORT_NAME: easel
  TAG: beta

jobs:
  export-windows:
    name: Windows Export
    runs-on: ubuntu-24.04
    container:
      image: docker://barichello/godot-ci:4.7.1
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          lfs: true

      - name: Setup
        run: |
          mkdir -v -p ~/.config
          mkdir -v -p ~/.local/share/godot/export_templates
          mv /root/.config/godot ~/.config/godot
          mv /root/.local/share/godot/export_templates/${GODOT_VERSION}.stable ~/.local/share/godot/export_templates/

      - name: Import resources once
        continue-on-error: true
        run: godot --headless -v --import

      - name: Windows Build
        run: |
          mkdir -v -p build/${EXPORT_NAME}-Windows-64bit
          mkdir -v -p build/${EXPORT_NAME}-Windows-arm64
          godot --headless -v --export-release "Windows Desktop 64bit" ./build/${EXPORT_NAME}-Windows-64bit/${EXPORT_NAME}.exe
          godot --headless -v --export-release "Windows Desktop arm64" ./build/${EXPORT_NAME}-Windows-arm64/${EXPORT_NAME}.exe

      - name: Copy easel_app_data folder
        run: |
          rm -f ./easel_app_data/.gdignore
          cp -R ./easel_app_data ./build/${EXPORT_NAME}-Windows-64bit
          cp -R ./easel_app_data ./build/${EXPORT_NAME}-Windows-arm64
          cp -R ./easel_app_data ./build/easel_app_data

      - name: Build installer
        run: |
          apt-get update
          apt-get install -y python3 python3-pip nsis
          pip3 install --break-system-packages polib
          python3 ./installer/utils/po2nsi.py -i ./installer/easel-app.nsi -o ./installer/easel-app_loc.nsi -p ./installer/po -l "English" -v
          makensis ./installer/easel-app_loc.nsi
          mv ./installer/*_setup.exe ./build/

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: windows_build
          path: ./build/

  export-linux:
    name: Linux Export
    runs-on: ubuntu-24.04
    container:
      image: docker://barichello/godot-ci:4.7.1
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          lfs: true

      - name: Setup
        run: |
          mkdir -v -p ~/.config
          mkdir -v -p ~/.local/share/godot/export_templates
          mv /root/.config/godot ~/.config/godot
          mv /root/.local/share/godot/export_templates/${GODOT_VERSION}.stable ~/.local/share/godot/export_templates/

      - name: Import resources once
        continue-on-error: true
        run: godot --headless -v --import

      - name: Linux Build
        run: |
          mkdir -v -p build/${EXPORT_NAME}-Linux-64bit
          mkdir -v -p build/${EXPORT_NAME}-Linux-arm64
          godot --headless -v --export-release "Linux 64bit" ./build/${EXPORT_NAME}-Linux-64bit/${EXPORT_NAME}.x86_64
          godot --headless -v --export-release "Linux arm64" ./build/${EXPORT_NAME}-Linux-arm64/${EXPORT_NAME}.arm64

      - name: Give execute permission
        run: |
          chmod +x ./build/${EXPORT_NAME}-Linux-64bit/${EXPORT_NAME}.x86_64
          chmod +x ./build/${EXPORT_NAME}-Linux-arm64/${EXPORT_NAME}.arm64

      - name: Copy easel_app_data folder
        run: |
          rm -f ./easel_app_data/.gdignore
          cp -R ./easel_app_data ./build/${EXPORT_NAME}-Linux-64bit
          cp -R ./easel_app_data ./build/${EXPORT_NAME}-Linux-arm64

      - name: Create tar.gz archive
        working-directory: ./build
        run: |
          tar zcvf ${EXPORT_NAME}-Linux-64bit.tar.gz ${EXPORT_NAME}-Linux-64bit
          tar zcvf ${EXPORT_NAME}-Linux-arm64.tar.gz ${EXPORT_NAME}-Linux-arm64

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: linux_build
          path: ./build/*.tar.gz

  export-mac:
    name: Mac Export
    runs-on: ubuntu-24.04
    container:
      image: docker://barichello/godot-ci:4.7.1
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          lfs: true

      - name: Setup
        run: |
          mkdir -v -p ~/.config
          mkdir -v -p ~/.local/share/godot/export_templates
          mv /root/.config/godot ~/.config/godot
          mv /root/.local/share/godot/export_templates/${GODOT_VERSION}.stable ~/.local/share/godot/export_templates/

      - name: Import resources once
        continue-on-error: true
        run: godot --headless -v --import

      - name: Mac Build
        run: |
          mkdir -v -p build
          godot --headless -v --export-release "macOS" ./build/${EXPORT_NAME}-Mac.zip

      - name: Bundle easel_app_data
        run: |
          apt-get update
          apt-get install -y zip unzip
          rm -f ./easel_app_data/.gdignore
          mkdir -v -p staging
          unzip -q ./build/${EXPORT_NAME}-Mac.zip -d staging
          cp -R ./easel_app_data staging/${EXPORT_NAME}.app/Contents/Resources
          chmod +x staging/${EXPORT_NAME}.app/Contents/MacOS/${EXPORT_NAME}
          rm ./build/${EXPORT_NAME}-Mac.zip
          cd staging
          zip -qry ../build/${EXPORT_NAME}-Mac.zip .

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: mac_build
          path: ./build/*.zip

  export-android:
    name: Android Export
    runs-on: ubuntu-24.04
    container:
      image: docker://barichello/godot-ci:4.7.1
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          lfs: true

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platform-tools build-tools;34.0.0 platforms;android-34 ndk;23.2.8568313'

      - name: Setup
        run: |
          mkdir -v -p ~/.config
          mkdir -v -p ~/.local/share/godot/export_templates
          mv /root/.config/godot ~/.config/godot
          mv /root/.local/share/godot/export_templates/${GODOT_VERSION}.stable ~/.local/share/godot/export_templates/

      - name: Create debug keystore
        run: |
          keytool -keyalg RSA -genkeypair -alias androiddebugkey -keypass android \
            -keystore ~/debug.keystore -storepass android -dname "CN=Android Debug,O=Android,C=US" \
            -validity 9999 -deststoretype pkcs12

      - name: Point Godot at the Android toolchain
        run: |
          test -d "${ANDROID_SDK_ROOT}/build-tools"
          SETTINGS=$(find ~/.config/godot -name 'editor_settings-*.tres' | head -n 1)
          cat >> "$SETTINGS" <<EOF
          export/android/android_sdk_path = "${ANDROID_SDK_ROOT}"
          export/android/java_sdk_path = "${JAVA_HOME}"
          export/android/debug_keystore = "$HOME/debug.keystore"
          export/android/debug_keystore_user = "androiddebugkey"
          export/android/debug_keystore_pass = "android"
          EOF

      - name: Import resources once
        continue-on-error: true
        run: godot --headless -v --import

      - name: Android Build
        run: |
          mkdir -v -p build
          godot --headless -v --install-android-build-template --export-debug "Android" ./build/${EXPORT_NAME}.apk

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: android_build
          path: ./build/*.apk
